# Let's Track v2 - 앱 분석 및 리뉴얼 계획

## 📋 목차
1. [현재 앱 분석](#1-현재-앱-분석)
2. [문제점 및 개선 필요사항](#2-문제점-및-개선-필요사항)
3. [v2 개선 방향](#3-v2-개선-방향)
4. [권장 기술 스택](#4-권장-기술-스택)
5. [새로운 아키텍처](#5-새로운-아키텍처)
6. [기능 로드맵](#6-기능-로드맵)
7. [구현 계획](#7-구현-계획)

---

## 1. 현재 앱 분석

### 1.1 기본 정보
- **앱 이름:** Let's Track (Extr)
- **개발 시기:** 2020년 2월
- **플랫폼:** iOS (iPhone/iPad)
- **다운로드:** 90+
- **최소 지원 버전:** iOS 9.0

### 1.2 기술 스택 (현재)
| 구분 | 기술 |
|------|------|
| 언어 | Swift |
| UI | UIKit + Storyboard |
| 데이터 저장 | Core Data |
| 차트 | Charts 3.4.0 |
| 달력 | FSCalendar 2.8.1 |
| 의존성 관리 | CocoaPods |

### 1.3 파일 구조 (현재)
```
letstrack/
├── AppDelegate.swift           # Core Data 스택 초기화
├── SceneDelegate.swift         # UIScene 생명주기
├── OverviewViewController.swift    # 메인 대시보드 (파이차트)
├── TransactionViewController.swift # 거래 목록
├── AddingViewController.swift      # 거래 추가
├── CellDetailViewController.swift  # 거래 상세/편집
├── MonthlyViewController.swift     # 월별 통계
├── CalendarViewController.swift    # 달력 뷰
├── SettingTableViewController.swift # 설정
├── TableViewCell.swift             # 커스텀 셀
├── TableViewCell.xib               # 셀 UI
├── Extr.xcdatamodeld/              # Core Data 모델
├── Main.storyboard                 # 메인 UI
└── LaunchScreen.storyboard         # 시작 화면
```

### 1.4 현재 기능
| 기능 | 설명 |
|------|------|
| ✅ 거래 추가 | 금액, 카테고리, 메모, 날짜 입력 |
| ✅ 거래 목록 | 테이블뷰로 모든 거래 표시 |
| ✅ 거래 편집/삭제 | 스와이프 삭제, 탭하여 편집 |
| ✅ 파이 차트 | 카테고리별 지출 비율 |
| ✅ 막대 차트 | 월별 지출 통계 |
| ✅ 달력 | 날짜별 거래 조회 |
| ✅ 검색 | 카테고리/메모로 검색 |
| ✅ 이메일 리포트 | 월별 지출 요약 전송 |
| ✅ 설정 | 앱 평가, 피드백, 데이터 초기화 |

### 1.5 데이터 모델 (현재)
```
ExpenseEntity
├── amount: Double (필수)
├── category: String (선택)
├── date: Date (필수)
└── note: String (선택)
```

### 1.6 카테고리 (9개 고정)
1. Food (음식)
2. Shopping (쇼핑)
3. Entertainment (엔터테인먼트)
4. Education (교육)
5. Transportation (교통)
6. Utility and Bill (공과금)
7. Housing (주거)
8. Car (자동차)
9. Other (기타)

---

## 2. 문제점 및 개선 필요사항

### 2.1 아키텍처 문제
| 문제 | 설명 | 심각도 |
|------|------|--------|
| 🔴 Massive ViewController | 뷰 로직과 비즈니스 로직이 혼합됨 | 높음 |
| 🔴 Singleton 남용 | `ManageData.shared.transactionVC` 패턴으로 강한 결합 | 높음 |
| 🟠 반복 코드 | 카테고리 판별 if-else 체인이 여러 곳에 중복 | 중간 |
| 🟠 테스트 불가 | 의존성 주입 없이 테스트 작성 어려움 | 중간 |
| 🟡 에러 처리 미흡 | 기본 try-catch만 사용 | 낮음 |

### 2.2 기술적 노후화
| 문제 | 현재 | 권장 |
|------|------|------|
| iOS 최소 버전 | iOS 9.0 | iOS 15.0+ |
| UI 프레임워크 | UIKit + Storyboard | SwiftUI |
| 반응형 프로그래밍 | 없음 | Combine |
| 동시성 | 없음 | Swift Concurrency (async/await) |
| 다크 모드 | 미지원 | 필수 지원 |

### 2.3 UX 문제
- ❌ 수입(Income) 기능 없음 - 지출만 기록 가능
- ❌ 예산 설정 기능 없음
- ❌ 반복 거래 기능 없음
- ❌ 다중 통화 미지원
- ❌ 카테고리 커스터마이징 불가
- ❌ 데이터 백업/복원 없음
- ❌ 위젯 없음
- ❌ 한국어 미지원

### 2.4 코드 품질 문제
```swift
// ❌ 나쁜 예: 카테고리 색상 판별 (반복되는 if-else)
if category == "Food" {
    return UIColor(named: "food")!
} else if category == "Shopping" {
    return UIColor(named: "shopping")!
} // ... 9번 반복

// ❌ 나쁜 예: Singleton으로 ViewController 공유
class ManageData {
    static let shared = ManageData()
    var transactionVC = TransactionViewController()
}
```

---

## 3. v2 개선 방향

### 3.1 핵심 원칙
1. **클린 아키텍처** - 관심사 분리, 테스트 가능한 구조
2. **SwiftUI 우선** - 선언적 UI로 생산성 향상
3. **사용자 중심** - 직관적이고 빠른 UX
4. **미니멀리즘** - 필수 기능만, 복잡함 배제
5. **현지화** - 한국어/영어 지원

### 3.2 기능 분류

#### ✅ 유지할 기능
- 거래 추가/편집/삭제
- 카테고리별 분류
- 파이 차트 (개선)
- 월별 통계 (개선)
- 검색 기능

#### ❌ 제거할 기능
- 이메일 리포트 (사용률 낮음, 대신 공유 기능으로 대체)
- 달력에서 거래 개수 표시 (복잡함)
- 앱 내 피드백 이메일 (App Store 리뷰로 대체)

#### ➕ 새로 추가할 기능
| 기능 | 우선순위 | 설명 |
|------|----------|------|
| 수입 기록 | 🔴 필수 | 수입/지출 구분하여 잔액 표시 |
| 예산 설정 | 🔴 필수 | 월별/카테고리별 예산 한도 |
| 커스텀 카테고리 | 🟠 높음 | 사용자가 카테고리 추가/편집 |
| 다크 모드 | 🟠 높음 | 시스템 설정 연동 |
| 위젯 | 🟠 높음 | 홈 화면에서 빠른 확인/추가 |
| iCloud 동기화 | 🟡 중간 | 기기 간 데이터 동기화 |
| 반복 거래 | 🟡 중간 | 월세, 구독료 등 자동 입력 |
| 데이터 내보내기 | 🟡 중간 | CSV/Excel 내보내기 |
| 다중 통화 | 🟢 낮음 | 여행 시 외화 기록 |

---

## 4. 권장 기술 스택

### 4.1 새로운 기술 스택
| 구분 | 기술 | 이유 |
|------|------|------|
| **언어** | Swift 5.9+ | 최신 언어 기능 활용 |
| **UI** | SwiftUI | 선언적 UI, 빠른 개발 |
| **아키텍처** | MVVM + Clean Architecture | 테스트 용이, 유지보수성 |
| **데이터** | SwiftData | Core Data의 현대적 대체 |
| **반응형** | Combine + @Observable | SwiftUI와 자연스러운 통합 |
| **동시성** | Swift Concurrency | async/await로 깔끔한 비동기 |
| **DI** | 수동 DI 또는 Factory | 경량 의존성 주입 |
| **차트** | Swift Charts | Apple 공식, SwiftUI 네이티브 |
| **테스트** | XCTest + Swift Testing | 단위/통합 테스트 |
| **최소 버전** | iOS 17.0+ | SwiftData, Swift Charts 활용 |

### 4.2 의존성 최소화
```
외부 라이브러리: 없음 (목표)

Apple 프레임워크만 사용:
- SwiftUI
- SwiftData
- Swift Charts
- WidgetKit
- CloudKit (iCloud 동기화)
```

---

## 5. 새로운 아키텍처

### 5.1 레이어 구조 (간소화된 클린 아키텍처)

> **참고:** 프로젝트 규모에 맞게 UseCase 레이어를 제거하고 간소화된 구조를 적용했습니다.

```
┌─────────────────────────────────────────────────┐
│                 Presentation Layer               │
│  ┌─────────────┐  ┌─────────────┐  ┌──────────┐ │
│  │   Views     │  │ ViewModels  │  │ Widgets  │ │
│  │  (SwiftUI)  │  │ (@Observable)│  │          │ │
│  └─────────────┘  └─────────────┘  └──────────┘ │
│         │               │                        │
│         └───────────────┼────────────────────────┤
│                         ↓                        │
├─────────────────────────────────────────────────┤
│                  Domain Layer                    │
│  ┌─────────────┐  ┌─────────────────────────┐   │
│  │   Models    │  │  Repository Protocols   │   │
│  │ (Entities)  │  │   (추상화 인터페이스)     │   │
│  └─────────────┘  └─────────────────────────┘   │
├─────────────────────────────────────────────────┤
│                   Data Layer                     │
│  ┌─────────────────────────────────────────────┐│
│  │  Repository Implementations (SwiftData)     ││
│  │  - TransactionRepository                    ││
│  │  - CategoryRepository                       ││
│  │  - BudgetRepository                         ││
│  │  - RecurringTransactionRepository           ││
│  └─────────────────────────────────────────────┘│
├─────────────────────────────────────────────────┤
│                Infrastructure Layer              │
│  ┌──────────────┐  ┌────────────┐  ┌──────────┐ │
│  │ DI Container │  │  Services  │  │Utilities │ │
│  └──────────────┘  └────────────┘  └──────────┘ │
└─────────────────────────────────────────────────┘
```

### 5.1.1 아키텍처 핵심 원칙

| 원칙 | 설명 |
|------|------|
| **Protocol 기반 추상화** | Repository는 Protocol로 정의되어 테스트 용이 |
| **의존성 역전** | ViewModel이 구체 클래스가 아닌 Protocol에 의존 |
| **에러 핸들링** | 모든 Repository 메서드에서 `throws` 처리 |
| **다국어 지원** | 에러 메시지 포함 전체 로컬라이제이션 |

### 5.2 현재 폴더 구조 (구현 완료)
```
LetsTrack/
├── Application/                    # 앱 진입점
│   ├── LetsTrackApp.swift         # SwiftUI App, ModelContainer
│   └── ContentView.swift          # 메인 TabView
│
├── Domain/                         # 비즈니스 로직 레이어
│   ├── Models/                    # SwiftData 엔티티
│   │   ├── Transaction.swift
│   │   ├── Category.swift
│   │   ├── Budget.swift
│   │   ├── RecurringTransaction.swift
│   │   ├── TransactionType.swift
│   │   └── Currency.swift
│   │
│   └── Protocols/                 # Repository 인터페이스
│       ├── TransactionRepositoryProtocol.swift
│       ├── CategoryRepositoryProtocol.swift
│       ├── BudgetRepositoryProtocol.swift
│       └── RecurringTransactionRepositoryProtocol.swift
│
├── Data/                           # 데이터 접근 레이어
│   └── Repositories/              # Protocol 구현체
│       ├── TransactionRepository.swift
│       ├── CategoryRepository.swift
│       ├── BudgetRepository.swift
│       └── RecurringTransactionRepository.swift
│
├── Presentation/                   # UI 레이어
│   ├── Common/
│   │   ├── Components/            # 재사용 UI 컴포넌트
│   │   │   └── ReceiptPhotoPicker.swift
│   │   └── Extensions/            # 유틸리티 확장
│   │       ├── Date+Extensions.swift
│   │       ├── Color+Extensions.swift
│   │       └── Decimal+Extensions.swift
│   │
│   └── Features/                  # 기능별 모듈
│       ├── Dashboard/
│       │   ├── Views/
│       │   ├── ViewModels/
│       │   └── Components/
│       ├── Transactions/
│       ├── Statistics/
│       ├── Budget/
│       ├── Categories/
│       ├── Recurring/
│       └── Settings/
│
├── Infrastructure/                 # 인프라 레이어
│   ├── DI/
│   │   └── DIContainer.swift      # 의존성 주입 컨테이너
│   ├── Services/
│   │   └── NotificationService.swift
│   └── Utilities/
│       └── ImageCompressor.swift
│
├── Resources/                      # 리소스
│   ├── Assets.xcassets/
│   ├── en.lproj/Localizable.strings
│   └── ko.lproj/Localizable.strings
│
├── Widget/                         # 홈 화면 위젯
│   └── LetsTrackWidget.swift
│
└── Tests/                          # 테스트
    ├── UnitTests/
    └── IntegrationTests/
```

### 5.3 데이터 모델 (새로운)

```swift
// Transaction.swift
import SwiftData

@Model
final class Transaction {
    var id: UUID
    var amount: Decimal
    var type: TransactionType  // .income 또는 .expense
    var category: Category?
    var note: String
    var date: Date
    var isRecurring: Bool
    var recurringRule: RecurringRule?
    var createdAt: Date
    var updatedAt: Date

    // 관계
    @Relationship(inverse: \Category.transactions)
    var category: Category?
}

// Category.swift
@Model
final class Category {
    var id: UUID
    var name: String
    var icon: String        // SF Symbol 이름
    var colorHex: String
    var type: TransactionType
    var isDefault: Bool     // 기본 카테고리 여부
    var sortOrder: Int

    @Relationship
    var transactions: [Transaction]

    @Relationship
    var budget: Budget?
}

// Budget.swift
@Model
final class Budget {
    var id: UUID
    var amount: Decimal
    var period: BudgetPeriod  // .monthly, .weekly
    var category: Category?   // nil이면 전체 예산
    var startDate: Date
}

// Enums
enum TransactionType: String, Codable {
    case income
    case expense
}

enum BudgetPeriod: String, Codable {
    case weekly
    case monthly
    case yearly
}
```

---

## 6. 기능 로드맵 및 진행 상황

### Phase 1: MVP (핵심 기능) ✅ 완료
- [x] 프로젝트 초기 설정 (xcodegen)
- [x] SwiftData 모델 구현 (Transaction, Category, Budget, RecurringTransaction)
- [x] Repository 패턴 구현
- [x] 거래 추가/편집/삭제 (수입/지출)
- [x] 거래 목록 뷰 (검색, 필터링)
- [x] 대시보드 (잔액, 이번 달 요약)
- [x] 기본 카테고리 (13개: 지출 9개 + 수입 4개)
- [x] 다크 모드 지원 (설정에서 테마 선택 가능)
- [x] 한국어 지원

### Phase 2: 통계 & 시각화 ✅ 완료
- [x] 카테고리별 파이 차트 (Swift Charts)
- [x] 월별 막대 차트
- [x] 기간별 필터링 (월간/연간)
- [x] 상세 통계 뷰

### Phase 3: 예산 관리 ✅ 완료
- [x] 월별 예산 설정
- [x] 카테고리별 예산
- [x] 예산 진행률 시각화
- [x] 예산 초과 알림 (설정에서 활성화)

### Phase 4: 고급 기능 ✅ 완료
- [x] 커스텀 카테고리 (추가/편집/삭제/정렬)
- [x] 데이터 내보내기 (CSV)
- [x] 반복 거래 기능 (매일/매주/격주/매월/매년)
- [x] 알림 기능 (LocalNotification)

### Phase 5: 확장 ✅ 완료
- [x] 홈 화면 위젯 (WidgetKit) - Small, Medium 지원
- [x] 빌드 테스트 완료
- [ ] iCloud 동기화 (CloudKit) - 추후 구현 예정

---

## 7. 구현 계획

### 7.1 프로젝트 생성
```
1. Xcode에서 새 프로젝트 생성
   - iOS App
   - Interface: SwiftUI
   - Storage: SwiftData
   - Include Tests ✓

2. 프로젝트 설정
   - Deployment Target: iOS 17.0
   - Bundle ID: com.yourname.letstrack
   - Display Name: Let's Track
```

### 7.2 초기 구현 순서

#### Week 1: 기반 구축
1. 폴더 구조 생성
2. SwiftData 모델 정의
3. Repository 패턴 구현
4. 기본 카테고리 데이터

#### Week 2: 핵심 UI
1. 메인 탭 뷰 구조
2. 거래 추가 화면
3. 거래 목록 화면
4. 거래 상세/편집

#### Week 3: 대시보드 & 통계
1. 대시보드 뷰
2. Swift Charts 통합
3. 기간 필터

#### Week 4: 마무리
1. 설정 화면
2. 다크 모드
3. 현지화
4. 버그 수정 & 테스트

---

## 📝 참고 사항

### 기존 데이터 마이그레이션
- Core Data → SwiftData 마이그레이션 고려
- 기존 사용자를 위한 데이터 이전 기능 필요

### 디자인 가이드라인
- Apple Human Interface Guidelines 준수
- SF Symbols 활용
- 시스템 색상 사용 (다크 모드 호환)

### 테스트 전략
- ViewModel 단위 테스트
- Repository 통합 테스트
- UI 스냅샷 테스트

---

*마지막 업데이트: 2025-12-19*

---

## 8. 아키텍처 개선 사항 (2025-12-19)

### 8.1 적용된 클린 아키텍처 패턴

#### Repository Protocol 추가
- `TransactionRepositoryProtocol` - 거래 데이터 추상화
- `CategoryRepositoryProtocol` - 카테고리 데이터 추상화
- `BudgetRepositoryProtocol` - 예산 데이터 추상화
- `RecurringTransactionRepositoryProtocol` - 반복 거래 데이터 추상화

#### 에러 핸들링 개선
```swift
enum RepositoryError: Error, LocalizedError {
    case saveFailed(underlying: Error)
    case fetchFailed(underlying: Error)
    case deleteFailed(underlying: Error)
    case notFound
    case invalidData(reason: String)
}
```

#### ViewModel 개선
- Protocol 기반 의존성 주입
- `isLoading`, `errorMessage` 상태 추가
- `try-catch` 기반 에러 처리

#### DI Container
```swift
@MainActor
final class DIContainer {
    var transactionRepository: TransactionRepositoryProtocol
    var categoryRepository: CategoryRepositoryProtocol
    var budgetRepository: BudgetRepositoryProtocol
    var recurringTransactionRepository: RecurringTransactionRepositoryProtocol
}
```

### 8.2 제거된 항목
- `Domain/UseCases/` - 프로젝트 규모에 맞게 간소화
- `Features/` (root level) - 중복된 빈 폴더 제거

### 8.3 향후 개선 가능 사항
- [ ] Mock Repository 구현 (테스트용)
- [ ] UI에서 에러 메시지 표시 컴포넌트
- [ ] iCloud 동기화 (CloudKit)
